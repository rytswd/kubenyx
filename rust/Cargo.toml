[workspace]
resolver = "2"
members = [
    "kubenyx",
    "kubenyx-pki",
    "kubenyx-ready",
    "etcd-mem",
    "kubenyx-snap",
    "kubenyx-clockstep",
    "kubenyx-lb",
    "kubenyx-tls",
]

[profile.release]
lto = true
codegen-units = 1
strip = true
